% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mbspls_model_summary.R
\name{mbspls_model_summary}
\alias{mbspls_model_summary}
\title{Tidy summaries for MB-sPLS, MB-sPLS-XY, and MB-sPCA fits}
\usage{
mbspls_model_summary(
  x,
  id = NULL,
  include_weights = TRUE,
  include_stability = TRUE
)
}
\arguments{
\item{x}{A fitted \link[mlr3pipelines:mlr_learners_graph]{mlr3pipelines::GraphLearner}, \code{\link[=PipeOpMBsPLS]{PipeOpMBsPLS()}},
\code{\link[=PipeOpMBsPLSXY]{PipeOpMBsPLSXY()}}, or \code{\link[=PipeOpMBsPCA]{PipeOpMBsPCA()}}.}

\item{id}{Optional pipeop id to use when \code{x} is a \code{GraphLearner} containing
multiple multi-block nodes.}

\item{include_weights}{Logical; include the feature-level weights table.}

\item{include_stability}{Logical; include bootstrap stability summaries when
they are available.}
}
\value{
A named list of \code{data.table} objects.
}
\description{
\code{mbspls_model_summary()} extracts compact, tidy summaries from fitted
\code{PipeOpMBsPLS}, \code{PipeOpMBsPLSXY}, \code{PipeOpMBsPCA}, or \code{GraphLearner} objects.

The helper is intended for reporting, interpretation, and handoff to domain
experts. It turns the internal model state into tables that are easy to review
in notebooks, manuscripts, clinical reports, or downstream dashboards.

The returned list always includes:
\itemize{
\item \code{overview}: high-level model metadata,
\item \code{components}: one row per latent component,
\item \code{blocks}: one row per component-block pair,
\item \code{weights}: one row per feature (optional).
}

For MB-sPLS graphs with a bootstrap-selection node, the list also includes a
\code{stability} table if \code{include_stability = TRUE}.
}
\details{
Summarize a fitted multi-block model
}
\examples{
task = mlr3::tsk("mbspls_synthetic_blocks")
gl = mbspls_graph_learner(
  task = task,
  learner = mlr3::lrn("clust.kmeans", centers = 2L),
  ncomp = 1L,
  bootstrap = FALSE,
  bootstrap_selection = FALSE,
  B = 1L,
  val_test = "none"
)
# gl$train(task)
# mbspls_model_summary(gl)

}
