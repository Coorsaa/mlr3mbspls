% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mb_task_overview.R
\name{mb_task_overview}
\alias{mb_task_overview}
\title{Block-wise quality-control summary for TaskMultiBlock objects}
\usage{
mb_task_overview(
  task,
  rows = NULL,
  blocks = NULL,
  include_target = TRUE,
  top_levels = 5L
)
}
\arguments{
\item{task}{A task created with \code{\link[=TaskMultiBlock]{TaskMultiBlock()}} or any \link[mlr3:Task]{mlr3::Task} that
carries multiblock metadata in \code{task$blocks} or \code{task$extra_args$blocks}.}

\item{rows}{Optional row ids to summarize. Defaults to all task rows.}

\item{blocks}{Optional block subset. Either a character vector of block names
or a named block mapping.}

\item{include_target}{Logical; summarize the target if the task is
supervised.}

\item{top_levels}{Maximum number of classification levels to print in the
target summary. All levels are still returned in \code{target_distribution}.}
}
\value{
A named list with \code{data.table} components: \code{overview}, \code{blocks},
\code{target}, \code{target_distribution}, and \code{issues}.
}
\description{
\code{mb_task_overview()} creates a compact, analysis-ready overview of a
multi-block task. It is designed as an early quality-control step before
fitting MB-sPLS, MB-sPLS-XY, or MB-sPCA pipelines.

The returned object contains:
\itemize{
\item \code{overview}: task-level dimensions and complete-case rates,
\item \code{blocks}: one row per block with feature counts, type counts, missingness,
and constant-feature counts,
\item \code{target}: a one-row summary for supervised tasks,
\item \code{target_distribution}: class counts for classification tasks,
\item \code{issues}: a compact table of actionable warnings/notes.
}

This helper is particularly useful in clinical, social-science, and
multi-omics workflows where block imbalance, missingness, and low-information
variables should be checked explicitly before resampling or interpretation.

For \code{TaskMultiBlock()} objects, \code{task$overview()} is the primary interface.
\code{mb_task_overview(task)} is retained as a convenience wrapper for ad hoc code
and backward compatibility.
}
\details{
Summarize a multi-block task
}
\examples{
task = mlr3::tsk("mbspls_synthetic_classif")
qc = task$overview()
qc$overview
qc$blocks
qc$issues

}
